

services:

  qdrant:
    image: qdrant/qdrant

    container_name:
      github_rag_qdrant

    ports:
      - "6333:6333"

    volumes:
      - qdrant_storage:/qdrant/storage

volumes:
  qdrant_storage: